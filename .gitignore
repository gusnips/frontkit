node_modules
dist
*.tsbuildinfo
.DS_Store

# The token compile check builds here. Cleared on success, kept on failure so the CSS
# that broke is there to read.
tokens/.compile-check

# Copied in at publish time from the repo root, so the licence has one source of truth.
# The READMEs are NOT here: each package has its own, committed, because they are what npm
# shows and a reader landing on @gusnips/tokens needs a CSS import, not `bun add @gusnips/react`.
tokens/LICENSE
http/LICENSE
locale/LICENSE
react/LICENSE
vite/LICENSE

# Internal: names private codebases. Stays on disk, out of the repo.
ROADMAP.local.md
